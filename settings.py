video_settings = {
    'window_fullscreen': True,  # Either or not the game is in fullscreen
    'window_size': (1920, 1080),  # The window size (if not in fullscreen)
    'window_vsync': True,  # Eiter or not the game uses NVidia VSync
    'video_max_fps': 60,  # The max amount of frames/s
    'video_quality': 'high',  # The entities texture quality
    'video_fov': 2,  # The camera's FOV
    'video_render_distance': None,  # The entity loading distance (not working yet)
}

dev_settings = {
    'ursina_splash': False,
                }